import { Theme } from '@emotion/react';
import { SxProps } from '@mui/material';

export const loaderBoxSx: SxProps<Theme> = {
  width: '100%',
  backgroundColor: null,
  justifyItems: 'center',
  alignContent: 'center',
  minHeight: '100vh',
};
