import { Theme } from "@emotion/react";
import { SxProps } from "@mui/material";
import { theme } from "../../theme";

export const dialogContentTextSx: SxProps<Theme> = {
  color: theme.palette.primary.dark,
  fontFamily: "'Georgia', serif",
};